#include <stdio.h>

/**
 * main -Prints thew numbers since 0 to 9
 *
 * Returns: Always (Success)
 */
int main(void)
{
	char c;

	for (n = '0'; n <= '9'; n++)
	{
		putchar(n);
	}

	putchar('\n');

	return (0);
}
